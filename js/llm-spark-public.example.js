/**
 * 可选：全员共用的星火 APIPassword（会提交到 GitHub，任何人可见并可能盗用免费额度）
 *
 * 若仅自己游玩：不要创建本文件，改在「系统设置」填写即可（仅存本机浏览器）。
 * 若希望所有访客免配置：复制本文件为 llm-spark-public.js，填入 APIPassword 并 push。
 */
export const SPARK_API_PASSWORD = '';
