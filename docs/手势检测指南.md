# 手势检测指南

节点 **24**（地球 · 伸手）视频播完后，会自动弹出 **生物特征验证** 弹窗，通过摄像头 + MediaPipe 识别手势：

| 手势 | 剧情选项 | 跳转节点 |
|------|----------|----------|
| 手枪状（食指伸出，其余手指握拳） | 警惕 | 26 |
| 伸手 / 张开手掌（四指伸直） | 握上 | 27 |

保持同一手势约 **1 秒**，进度条填满后自动确认。

---

## 测试入口

开始界面右下角 → **手势实时检测**（在「与新人类对话」上方）

- 不推进剧情，仅测试摄像头与识别效果
- 需要浏览器允许 **摄像头权限**
- 建议使用 **Chrome / Edge**，且通过 **HTTPS** 或 `http://localhost` 访问

---

## 备用操作

1. **跳过手势检测** → 显示鼠标按钮
2. 约 **28 秒** 未识别成功 → 自动显示鼠标按钮
3. 手动点击 **警惕** / **握上** 与手势效果相同

---

## 技术说明

- 使用 Google **MediaPipe Hand Landmarker**（从 jsDelivr CDN 加载）
- **无需** 本地 `Mediapipe` 文件夹；若你添加了该目录但为空，不影响运行
- 首次打开会从 CDN 下载模型（约数 MB），需联网

### 若摄像头无法启动

- 确认网站为 HTTPS（GitHub Pages 可以）
- 本地请用：`python -m http.server 8080`，访问 `http://localhost:8080`
- 不要用 `file://` 直接打开 `index.html`

### 若 MediaPipe 加载失败

- 检查网络能否访问 `cdn.jsdelivr.net` 和 `storage.googleapis.com`
- 打开浏览器开发者工具（F12）→ Console 查看报错

---

## 推送到 GitHub

```powershell
cd "C:\Users\Mon3tr\Desktop\GamePrograming\Mini works\wandering-baby-plan"
git add .
git commit -m "新增 MediaPipe 手势检测：节点24抉择与测试入口"
git push origin main
```

推送后等待 GitHub Pages 部署完成（约 1–2 分钟），再访问线上地址测试。

**注意：** 手势功能不需要上传任何 MediaPipe 本地文件，全部走 CDN。
